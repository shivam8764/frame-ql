# frame-ql
A mini SQL-like data engine from scratch that supports parsing CSV files, storing data in our own DataFrame class, and providing fundamental operations: filtering, projection, group-by, aggregation, and join.
